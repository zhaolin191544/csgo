# PaddleOCR 昇腾平台性能摸测报告


**测试对象**:PP-OCRv5_server（det + rec）on Atlas 800I A2 / Ascend 910B4-1   西安绿区110.21.1.23

---

## 摘要

本次摸测围绕两项任务展开:(1) 基于客户数据集进行 PaddleOCR 性能摸测;(2) 算力切分与多实例最高吞吐测试。

**主要结论:**



| 指标 | 结果 |
|---|---|
| 单实例吞吐 | **1.21 QPS**（三次重复,变异系数 0.67%） |
| 单实例时延 | avg 821 ms / P50 827 ms / P99 1560 ms |
| **单卡最高吞吐** | **27.96 QPS**（约 38 实例） |
| 单卡实例上限 | 优化前 24 → 优化后 **~38**（受 HBM 容量限制） |
| **性能瓶颈** | **HBM 显存容量**,而非算力（拐点处 AICore 仅 48~54%） |
| 精度（om vs Paddle） | 平均字符一致率 **99.88%**,最低 99.17%,零推理失败 |
| 算力切分（vNPU） | **对本负载吞吐无正收益**,价值在隔离与 QoS |

**最有价值的两条工程发现:**

1. **官方文档建议的 `onnxsim` 步骤在动态 shape 场景下是错误的**,会导致约 35% 的真实业务图片推理失败。
2. **`ASCEND_OM_OUTPUTSIZE` 从 1024MB 降至 64MB**,单实例显存从 2686MB 降至 1726MB,单卡实例上限提升约 58%,而单实例性能无损失。这是本次投入产出比最高的一项优化——只改一个环境变量。

---

## 目录

1. [任务背景与目标](#1-任务背景与目标)
2. [测试环境](#2-测试环境)
3. [数据集分析](#3-数据集分析)
4. [实施过程与踩坑记录](#4-实施过程与踩坑记录)
5. [任务一:单实例摸测结果](#5-任务一单实例摸测结果)
6. [任务二:多实例吞吐与算力切分](#6-任务二多实例吞吐与算力切分)
7. [数据处理方法](#7-数据处理方法)
8. [结论](#8-结论)
9. [放弃或调整的工作项及原因](#9-放弃或调整的工作项及原因)
10. [局限性说明](#10-局限性说明)
11. [改进建议](#11-改进建议)
12. [附录](#12-附录)

---

## 1. 任务背景与目标

### 1.1 任务描述

1. **基于 PaddleOCR 在客户数据集上摸测**——观察服务器性能上限
2. **算力切分,多实例测试最高吞吐**

### 1.2 目标拆解

| 任务 | 需要回答的问题 | 交付物 |
|---|---|---|
| 任务一 | 单实例在真实数据上的端到端时延、吞吐、精度;瓶颈在哪 | 数据集画像 + 单实例基线 + 精度基线 + 瓶颈定位 |
| 任务二 | 单卡可以起多少实例、吞吐拐点在哪;算力切分是否有收益 | 吞吐爬坡曲线 + 最优配置 + vNPU 对照结论 |

### 1.3 参考文档

昇腾社区《PaddleOCR优化参考实践》
https://www.hiascend.com/developer/techArticles/20260313-1

环境搭建、镜像构建、模型转换流程均以该文档为基础,本报告在其之上补充数据集分析、瓶颈定位与多实例吞吐测试,并修正了文档中一处会导致功能性错误的建议（见 4.4 节）。

---

## 2. 测试环境

### 2.1 硬件

| 项 | 配置 |
|---|---|
| 服务器 | Atlas 800I A2 |
| NPU | 8 × Ascend 910B4-1,单卡 HBM 65536 MB |
| CPU | 256 核(aarch64) |
| 内存 | 约 500 GB |
| OS | openEuler 24.03 LTS SP3,kernel 6.6.0 |
| 驱动 / npu-smi | 26.0.rc1 |

### 2.2 NPU 与 NUMA 拓扑

| Device | Bus-Id | NUMA | 亲和 CPU |
|---|---|---|---|
| 0 | 0000:C1:00.0 | 6 | 192-223 |
| 1 | 0000:C2:00.0 | 6 | 192-223 |
| 2 | 0000:81:00.0 | 4 | 128-159 |
| 3 | 0000:82:00.0 | 4 | 128-159 |
| 4 | 0000:01:00.0 | 0 | 0-31 |
| 5 | 0000:02:00.0 | 0 | 0-31 |
| 6 | 0000:41:00.0 | 2 | 64-95 |
| 7 | 0000:42:00.0 | 2 | 64-95 |

**关键约束**:8 张卡仅映射到 4 个 NUMA 节点,每节点 32 核由 2 张卡共享,**全机亲和核仅 128 个,平均每卡 16 核**。这是整机满配时的主要制约因素。

### 2.3 软件栈

| 组件 | 版本 |
|---|---|
| CANN Toolkit | 8.5.0 |
| PaddleX | 3.4.2（已打动态 shape patch） |
| ultra-infer-npu | 1.2.0 |
| OpenCV | 4.10.0（含鲲鹏 KPCV 加速库） |
| Python | 3.11 |
| 基础镜像 | openEuler 24.03 LTS SP3 |

### 2.4 关键环境变量

```bash
export PADDLE_PDX_DISABLE_MODEL_SOURCE_CHECK=True
export ASCEND_OM_OUTPUTSIZE=64        # 单位 MB，最终采用值
export SOC=Ascend910B4-1
```

### 2.5 模型配置

| 模型 | 源 | atc input_shape | 说明 |
|---|---|---|---|
| PP-OCRv5_server_det | `inference.onnx` | `x:1,3,320~1408,320~1408` | **未过 onnxsim** |
| PP-OCRv5_server_rec | `inference.onnx` | `x:1,3,48,8~3200` | **未过 onnxsim**,batch 固定 1 |

产线配置 `limit_side_len: 960`,`rec batch_size: 4`,关闭文档方向分类与文本行方向分类。

---

## 3. 数据集分析

### 3.1 基本画像

数据集为 4 个目录、共 **104 张 PNG**,目录名为企业统一社会信用代码,内容为招投标文档扫描件。

| 目录 | 页数 | 平均文本行数 | 最大文本行数 |
|---|---|---|---|
| 913101095665640837 | 29 | 38.7 | 90 |
| 913501007869112788 | 15 | 51.1 | 103 |
| 91440000096926122N | 24 | 57.5 | 209 |
| 91440101304630351D | 36 | 38.7 | 100 |
| **合计** | **104** | **44.8** | **209** |

| 属性 | 值 |
|---|---|
| 尺寸 | 仅三种:1449×2048、2048×1449、1448×2048 |
| 通道模式 | 全部 RGB |
| 平均文件大小 | 830.7 KB |
| 损坏文件 | 0 |
| 长边分布 | 100% 落在 1920~2560 区间 |

宽高比 1449/2048 = 0.7075,即标准 A4 比例,长边 2048 约合 175 DPI。

### 3.2 关键洞察:真正的性能变量是文本密度

数据集尺寸高度统一,**图片分辨率不是性能变量**。真正决定单页耗时的是每页文本行数——det 检出多少个框,rec 就要执行多少次推理。

| 指标 | 值 |
|---|---|
| 每页文本行数 min / 中位 / P90 / max | 13 / 41 / 69 / 209 |
| 全集总文本行数 | 4661 |

这一认知直接改变了后续的测试设计:不再按目录分组测试（尺寸一致,无区分度）,转而以文本行数作为耗时建模的自变量。

### 3.3 rec 输入宽度分布

对全量 104 张图执行 det,统计裁剪后送入 rec 的文本行宽度（高度归一化至 48）:

![image-20260910185719350](C:\Users\z30086963\AppData\Roaming\Typora\typora-user-images\image-20260910185719350.png)

| 指标 | 值 |
|---|---|
| 文本框总数 | 4762 |
| min / P1 / 中位 | 3 / 34 / 185 |
| P99 / P99.9 / max | 1553 / 1930 / **2185** |
| 宽度 < 160 的占比 | **48%** |

这份分布是本次摸测中最关键的一张表——它同时解释了两个后来发生的故障（见 4.3、4.5 节）。招投标文档含大量表格单元格与短标签,近一半文本框宽度不足 160 像素;同时也存在接近 2200 像素的长文本行,跨度超过 60 倍。

---

## 4. 实施过程与踩坑记录

本节按时间顺序记录实际遇到的问题、定位过程与解决方案。**这部分占用了本次任务约 60% 的时间**,记录在此以供后续同类任务参考。

### 4.1 环境阶段:`dcmi module initialize failed. ret is -8005`

**现象**:执行 `npu-smi info` 查询芯片型号时报错。

**定位过程**:
1. 区分宿主机 / 容器 → 确认在容器内
2. 检查设备节点、驱动模块、动态库路径
3. 对比 `-8005`（DCMI 初始化失败）与 `-8020`（设备被占用）的语义差异

**根因**:容器缺少 `/usr/local/dcmi`、`/etc/ascend_install.info` 等必要挂载。

**解决**:安装 Ascend Docker Runtime,起容器时用 `-e ASCEND_VISIBLE_DEVICES=0,1,...` 自动注入设备与驱动,不再手写 `--device` 与 `-v` 列表。

**教训**:环境搭建应逐步验证——装驱动后立即 `npu-smi info`,装 runtime 后立即 `docker exec npu-smi info`。一路装完再统一验证,出问题无法定位是哪一环。

### 4.2 镜像构建阶段的三个陷阱

按官方文档 3.3 节构建 paddlex 镜像时遇到:

| 陷阱 | 现象 | 解决 |
|---|---|---|
| 包名与通配符不匹配 | `./Ascend-cann-910*-ops*.run: No such file or directory`,退出码 127 | 从 codeload 下载的 PaddleX 为 `v3.4.2.tar.gz`、OpenCV 为 `4.10.0.zip`,均需 `-O` 改名以匹配 Dockerfile 通配符 |
| 文件名含不可见字符 | `ls *.run` 只显示一个文件,但目录里确实有 2GB 的包 | 用 `ls -lab` 显示转义字符,按文件大小 `find -size +1G` 重命名 |
| 基础镜像 tag 不匹配 | `pull access denied for openeuler-24.03-lts-sp3` | `docker load` 后的镜像名不一定等于 Dockerfile 的 `FROM`,需 `docker tag` |

**建议改进**:将 Dockerfile 中的 CANN 安装与 ultra-infer 编译拆分为两个 RUN 层。当时二者在同一层,ops 包一处失败导致已成功的 CANN Toolkit 安装（约 10 分钟）需要重跑。

### 4.3 rec 模型:宽度下界过高导致 48% 数据失败

**现象**:冒烟测试首张图即报 `execute model failed, errorCode is 500002`,随后 Python 抛出 `ValueError: attempt to get argmax of an empty sequence`。

**定位**:`argmax of an empty sequence` 是**连锁反应**——推理失败返回空数组,后处理无法处理。真正的错误是上一行的 ACL 500002（参数无效）。

**根因**:初版按模型 `inference.yml` 的默认范围设置 `x:1~8,3,48,160~3200`,而实测 48% 的文本框宽度低于 160。

**解决**:全量扫描宽度分布后,下界调整为 8。

**教训**:模型自带的 shape 范围是通用默认值,**必须用真实数据的统计结果覆盖它**。

### 4.4 【核心发现】onnxsim 破坏动态 shape 模型

**现象**:调整下界后单张图通过,但全量 104 张测试中 **36 张失败**,且失败集中在第 4 个目录。

**排查历程**（这是本次耗时最长的一段）:

| 假设 | 验证方式 | 结果 |
|---|---|---|
| 输出内存不足 | `ASCEND_OM_OUTPUTSIZE` 从 256 提至 1024 | ❌ 无效,失败数量完全一致 |
| 长时间推理的资源累积 | 重启容器后重跑 | ❌ 无效,失败位置与文件完全相同 |
| 该目录图片尺寸特殊（含 1448 宽度） | 分别取 1448 与 1449 宽度的图单测 | ❌ 两者都失败 |
| 动态 batch 有问题 | 转固定 batch=1 的 om | ❌ 无效（后证实此次 ATC 因 `$SOC` 变量丢失而未执行成功） |
| **查 CANN 底层日志** | `ASCEND_GLOBAL_LOG_LEVEL=1`,读 `~/ascend/log/debug/plog/` | ✅ **定位** |

**底层错误**:

```
[BroadcastShape] shape [1600, 1, 120] and [40, 40, 120] cannot broadcast!
OpName:[Add.38]
InferShape failed, node type Add, name Add.38
```

**根因**:`onnxsim` 在简化模型时执行常量折叠,按静态输入假设将某个 reshape 的输出形状固化为 `[40, 40, 120]`。运行时实际序列长度不同,与另一分支的 `[1600, 1, 120]` 无法广播,`Add` 算子 InferShape 失败。

**验证**:
- 同一张图 Paddle 后端（CPU）正常输出 77 行文本 → 模型本身没问题
- 去掉 `onnxsim`、直接用原始 `inference.onnx` 转 om → 报错类型立刻改变为干净的 107000（超出 shape 范围）

**结论**:昇腾社区《PaddleOCR优化参考实践》3.4 节建议对 rec 模型执行 `onnxsim`。**该建议在动态 shape 场景下会导致功能性错误**,影响本数据集 34.6%(36/104) 的图片。**建议反馈至 PaddleX / CANN 侧。**

### 4.5 抽样估计 shape 范围导致的二次失败

去掉 onnxsim 后,报错变为:

```
memcpy d2d failed. buffer size is 1179648, inputs[i].Nbytes() is 1257408, errorCode is 107000
```

反算:`1179648 / 4 / 3 / 48 = 2048`（om 上界），`1257408 / 4 / 3 / 48 = 2183`（实际输入宽度）。

**根因**:第一次宽度探测脚本写的是 `files[:30]`,而文件按目录名排序,**前 30 张全部落在第 1 个目录**,未覆盖第 4 个目录的版式。抽样得出 max=1977,据此设定上界 2048;全量扫描的真实 max 为 2185。

**解决**:上界调整为 3200,全量回归通过 104/104。

**教训**:动态 shape 范围必须基于**全量数据**统计。数据集异质时,抽样会系统性低估极值。

### 4.6 【核心发现】`ASCEND_OM_OUTPUTSIZE` 的两个隐蔽行为

**行为一:单位是 MB,超范围会静默回退**

排错过程中曾设置 `ASCEND_OM_OUTPUTSIZE=524288000`（误以为单位是字节）与 `=1288`,两次均触发:

```
GetOutputSizeFromENV  ASCEND_OM_OUTPUTSIZE is invalid, use default outputSize
...
Current value:64
```

**超出合法范围时不报错退出,而是静默回退到默认值 64MB。** 这在排错期间造成了误导——以为提高了内存配额,实际反而降低了。

**行为二:该值是预分配的,直接决定单实例显存占用**

实测每实例 HBM 占用:

| ASCEND_OM_OUTPUTSIZE | 单实例 HBM | 单实例 QPS |
|---|---|---|
| 1024 MB | 2686 MB | 1.211 |
| 256 MB | 1918 MB | 1.211 |
| 64 MB | **1726 MB** | 1.208 |

差值精确等于配置差（2686−1918 = 768 = 1024−256;1918−1726 = 192 = 256−64）,可推出**固定基础占用约 1662 MB**。

由于本负载瓶颈正是 HBM 容量,**这一项直接决定单卡实例数上限**（见 6.3 节）。而三档配置下单实例 QPS 完全一致（1.208~1.211,差异在噪声范围内）,**说明降低该值不带来任何性能损失**。

### 4.7 其他值得记录的操作问题

| 问题 | 说明 |
|---|---|
| 环境变量重启容器后丢失 | `$SOC` 未写入 `.bashrc`,导致一次 ATC 静默失败（`--soc_version for ATC is empty`）,误判为"固定 batch 无效" |
| `atc` 输出文件带后缀 | 生成的是 `xxx_linux_aarch64.om`,需手动 `cp` 为 `inference.om` |
| 中文目录名导致终端命令截断 | 粘贴含中文路径的命令时出现 `$'\344cp': command not found` |
| `paddlex` 命令不在 PATH | `import paddlex` 正常但 `which paddlex` 为空,改用 `python -m paddlex` |
| 压测脚本 `wait` 无参数导致卡死 | `wait` 会等待所有后台进程,包括 `while true` 的监控脚本,须改为 `wait $PIDS` |
| CPU 采集脚本 `sed` 未生效 | 用 `^CPU=` 匹配行首,但目标行在循环内有缩进,两次修改均静默失败,导致全程 `cpu_pct=0` |

### 4.8 服务器资源协调

摸测开始前发现 8 张卡被一个 vLLM 张量并行作业占满（HBM 56GB/65GB）。协调后获得整机独享。

**处理原则**:优先 `docker stop` 优雅停止而非 `kill -9`,避免 HBM 不释放;停止后验证 `npu-smi info` 中 HBM 归零。

---

## 5. 任务一:单实例摸测结果

### 5.1 单实例基线

配置:`limit_side_len=960`,`rec batch_size=4`,`taskset -c 1-31`,500 张样本（104 张 × loop 10 取前 500）,预热 30 张。

| 轮次 | QPS | avg (ms) | P50 | P90 | P99 | max |
|---|---|---|---|---|---|---|
| base_r1 | 1.216 | 822.59 | 832.61 | 1201.22 | 1581.14 | 2805.03 |
| base_r2 | 1.203 | 831.44 | 838.51 | 1219.43 | 1634.88 | 3105.94 |
| base_final | 1.218 | 821.07 | 826.52 | 1197.30 | 1559.58 | 2779.13 |
| **均值** | **1.212** | **825.03** | 832.55 | 1205.98 | 1591.87 | — |
| 标准差 | 0.0081 | — | — | — | — | — |
| **变异系数** | **0.67%** | — | — | — | — | — |

三次重复变异系数 0.67%,测试稳定性良好。

### 5.2 耗时构成分析

对全量 104 张单独计时,以文本行数为自变量做线性回归:

![image-20260910185807946](C:\Users\z30086963\AppData\Roaming\Typora\typora-user-images\image-20260910185807946.png)

```
单页耗时 ≈ 398.6 ms + 12.50 ms × 文本行数        R² = 0.681
```

| 组成 | 典型页（45 行） | 占比 |
|---|---|---|
| 固定开销（det 推理 + PNG 解码 + resize + DB 后处理 + crop） | 399 ms | 41% |
| rec 推理（随行数线性增长） | 562 ms | 59% |

**解读:**

- **固定开销 399 ms 中,det 推理仅占几十毫秒,其余 300 ms+ 是 CPU 单线程工作**——PNG 解码（830KB、2048×1449）、resize、DB 后处理（轮廓提取）、以及 45 个文本框的裁剪与透视变换。这部分完全不使用 NPU。
- **R² = 0.681 偏低**,说明文本行数只能解释 68% 的耗时方差。剩余部分来自文本行宽度分布（跨度 34~2185）导致的动态 shape 档位切换开销。
- 每行 12.50 ms 的 rec 耗时对于 SVTR 小模型偏高,同样指向动态档位切换开销。

### 5.3 资源利用率（单实例）

| 资源 | avg | max |
|---|---|---|
| AICore | 2.9% | 14.0% |
| HBM | 9% | 9% |
| 单实例 HBM 绝对占用 | 2686 MB（OUT=1024）→ 1726 MB（OUT=64） |

**单实例状态下 NPU 与 CPU 均处于极低负载**。这不是"算力不足",而是单进程串行等待——每个环节都在等上一环节,硬件大部分时间空转。这个观察直接决定了任务二的方向:**多实例扩展空间极大**。

### 5.4 精度校验

以 Paddle 后端（CPU,`use_hpip=False`）输出作为参考答案,与 om 后端做字符级一致性比对。

| 项 | 值 |
|---|---|
| 比对样本 | 30 张（104 张全集中均匀抽样,覆盖 4 个目录） |
| 完全一致 | 21 / 30 (70.0%) |
| **平均字符一致率** | **99.88%** |
| 最低一致率 | 99.17% |
| 推理失败 | 0 |

差异最大的 5 个样本一致率分别为 99.17%、99.23%、99.44%、99.67%、99.69%,差异均为个别字符级别,无整行漏检或错行。

**结论:om 后端精度可接受,可用于生产。**

### 5.5 NUMA 绑核对照（单实例）

| 绑核策略 | CPU 范围 | QPS | avg (ms) |
|---|---|---|---|
| 跨 NUMA | 1-4（NUMA 0） | 1.202 | 831.71 |
| 亲和 NUMA | 192-195（NUMA 6,卡 0 亲和） | 1.215 | 823.23 |
| 差异 | | **+1.1%** | −1.0% |

差异 1.1%,在噪声范围内。原因是单实例下 CPU 与 AICore 利用率均极低,内存带宽无压力,跨 NUMA 的额外延迟被大量空闲时间掩盖。

**这一结果对多实例测试有实际影响**:卡 0 亲和的 NUMA 6 仅 32 核,若严格亲和,24 实例每个仅分到 1.3 核;而跨 NUMA 宽绑核可用 128 核。在本负载下,**多分配 CPU 核的收益大于跨 NUMA 的损失**,因此后续多实例测试采用宽绑核策略。

---

## 6. 任务二:多实例吞吐与算力切分

### 6.1 单卡多实例爬坡

![image-20260910185827112](C:\Users\z30086963\AppData\Roaming\Typora\typora-user-images\image-20260910185827112.png)

**第一组（`ASCEND_OM_OUTPUTSIZE=1024`,每实例 4 核,`--limit 100 --loop 5 --warmup 20`）:**

| 实例数 | 聚合 QPS | 单实例 QPS | 扩展效率 | 单实例衰减 | AICore avg | HBM |
|---|---|---|---|---|---|---|
| 1 | 1.21 | 1.207 | 100% | — | 2.9% | 9% |
| 2 | 2.31 | 1.156 | 95.6% | −4.2% | — | — |
| 4 | 4.45 | 1.111 | 92.1% | −8.0% | — | — |
| 8 | 8.31 | 1.031 | 86.0% | −14.6% | ~23% | — |
| 16 | 15.13 | 0.945 | 78.3% | −21.7% | 41.8% | 69% |
| **24** | **20.47** | 0.853 | 70.7% | −29.3% | 53.6% | **98%** |
| 32 | 20.02 | 0.834 | 69.1% | −30.9% | 52.4% | 99% |

**拐点出现在 24 实例,单卡上限 20.47 QPS。**

**第二组（`ASCEND_OM_OUTPUTSIZE=64`,`--limit 60 --loop 3 --warmup 15`）:**

| 启动实例数 | 存活实例数 | 聚合 QPS | 单实例 QPS | avg (ms) | P99 max | AICore avg | HBM |
|---|---|---|---|---|---|---|---|
| 24 | 24 | 18.75 | 0.781 | 1281 | 3240 | 52.5% | 66% |
| 40 | 40 | 24.47 | 0.612 | 1636 | 4610 | 48.2% | 99% |
| 56 | **38** | **27.96** | 0.736 | 1371 | 3869 | 48.2% | 99% |
| 72 | **36** | 23.65 | 0.657 | 1541 | 4093 | 48.1% | 99% |

56 与 72 实例组中,分别只有 38 与 36 个实例成功写出结果——**超出部分因显存不足在初始化阶段即失败**。这两行的聚合值是按存活实例数加总的,不代表 56/72 实例的真实吞吐,但**反证了单卡实例上限约为 38**。

24 与 40 两档做过重复验证（18.74 / 24.48 vs 18.75 / 24.47），重复性极佳。

**单卡最高实测吞吐:27.96 QPS,约 38 实例。**

### 6.2 瓶颈定位:HBM 而非算力

![image-20260910185846120](C:\Users\z30086963\AppData\Roaming\Typora\typora-user-images\image-20260910185846120.png)

这是本次摸测最重要的一张图。两组数据均显示:

- **AICore 利用率长期停留在 48~54%,从未接近饱和**
- **HBM 在拐点处达到 98~99%**
- 拐点位置与 HBM 饱和位置完全重合

**结论:本负载的单卡实例数上限由 HBM 容量决定,而非算力。**

理论推算与实测的吻合度:

| ASCEND_OM_OUTPUTSIZE | 单实例 HBM | 理论上限（62.1GB 可用） | 实测上限 |
|---|---|---|---|
| 1024 MB | 2686 MB | 23 | **24** ✓ |
| 256 MB | 1918 MB | 32 | 未测 |
| 64 MB | 1726 MB | 36 | **~38** ✓ |

### 6.3 显存优化的效果

![image-20260910185929195](C:\Users\z30086963\AppData\Roaming\Typora\typora-user-images\image-20260910185929195.png)

将 `ASCEND_OM_OUTPUTSIZE` 从 1024 MB 降至 64 MB:

| 指标 | 优化前 | 优化后 | 变化 |
|---|---|---|---|
| 单实例 HBM | 2686 MB | 1726 MB | **−35.7%** |
| 单卡实例上限 | 24 | ~38 | **+58%** |
| 单实例 QPS | 1.211 | 1.208 | −0.2%（噪声内） |
| **单卡峰值吞吐** | **20.47** | **27.96** | **+36.6%** |
| 全量回归 | 104/104 | 104/104 | 无退化 |

**这是本次投入产出比最高的一项优化**:只修改一个环境变量,不改模型、不影响精度、不损失单实例性能,单卡吞吐提升 36.6%。

原值 1024 MB 是排错阶段为规避 `Model infer failed with dynamic shape` 而设置的保守值。按 rec 实际输出规模估算（batch 1 × 序列长度 × 字典 18000 × 4B）,64 MB 已经足够,全量回归 104/104 验证了这一点。

### 6.4 时延代价

![image-20260910185908661](C:\Users\z30086963\AppData\Roaming\Typora\typora-user-images\image-20260910185908661.png)

并发提升带来的时延增长（单实例 QPS 衰减的另一面）:

| 实例数 | 单实例 QPS | 相对单实例基线 |
|---|---|---|
| 1 | 1.207 | 100% |
| 8 | 1.031 | 85.4% |
| 24 | 0.853 | 70.7% |
| 40 | 0.612 | 50.7% |

在 40 实例配置下,单请求平均时延从 828 ms 增至 1636 ms,P99 最大值达 4610 ms。

**这意味着实际部署时存在吞吐与时延的取舍**:
- 追求最高吞吐 → 约 38 实例,单请求时延约 1.4 s,P99 接近 4 s
- 追求时延可控 → 8~16 实例,吞吐 8~15 QPS,P99 控制在 2 s 内

具体取哪个点,取决于客户的 SLA 要求。

### 6.5 算力切分（vNPU）

#### 6.5.1 硬件支持的切分模板

```
vir10_3c_32g:  AICore 10,  Memory 32GB,  AICPU 3
vir05_1c_16g:  AICore  5,  Memory 16GB,  AICPU 1
```

整卡为 20 AICore / 64GB,因此仅支持 1/2 与 1/4 两种切分粒度。

#### 6.5.2 实际操作与遇到的阻塞

已完成:
- 切换 `vnpu-mode -d 1`（docker 模式）
- 成功创建 vNPU（`vir10_3c_32g`,Vnpu ID = 100）
- 设备节点 `/dev/vdavinci100` 正常生成

**未完成:vNPU 环境下推理进程无法启动。**

| 尝试方式 | 结果 |
|---|---|
| `ASCEND_VISIBLE_DEVICES=100` | runtime 去找 `/dev/davinci100`,报 `failed to get /dev/davinci100 info : 0x2` |
| 手动 `--device=/dev/vdavinci100` + 完整挂载 | 容器可启动,但 `npu-smi info` 报 -8005,推理报 `acl init failed, errorCode = 500000` |

`npu-smi info -t info-vnpu` 显示该 vNPU 的 `Container ID` 为全零、`Status` 为 0,即**未被任何容器认领**。推测 Ascend Docker Runtime 在启动容器时需完成容器 ID 与 vgroup 的绑定注入,手动挂载设备节点跳过了这一步,导致 ACL 无法初始化。

受任务时间限制,未继续排查 runtime 的 vNPU 设备标识格式。

#### 6.5.3 基于已测数据的结论

尽管缺少 vNPU 压测数据,**基于已有实测事实仍可给出明确结论**:

**已实测的关键事实:**
1. 拐点处 AICore 利用率仅 48~54%,**算力不是瓶颈**
2. 单卡实例上限由 HBM 容量决定,每实例需 1726 MB
3. 单卡峰值吞吐 27.96 QPS,约 38 实例

**推算:**

| 方案 | 可用显存 | AICore | 实例上限 | 预期聚合吞吐 |
|---|---|---|---|---|
| 物理卡 | 62.1 GB | 20 | ~38 | **27.96**（实测） |
| vir10_3c_32g × 1 | 32 GB | 10 | ~18 | ~14（推算） |
| vir10_3c_32g × 2 | 32 GB × 2 | 10 × 2 | ~36 | ≤27.96（推算） |
| vir05_1c_16g × 4 | 16 GB × 4 | 5 × 4 | ~9 × 4 = 36 | ≤27.96（推算） |

**结论:算力切分对本负载的吞吐无正收益。**

理由:
- **显存侧**:切分后各份显存之和不超过整卡,加上切分本身的元数据开销,实例总数只减不增
- **算力侧**:实测所需算力约为整卡的 50%（折合约 10 AICore）。vir10 提供 10 AICore,恰好在临界点;vir05 仅 5 AICore,将成为新的瓶颈
- **调度侧**:切分后各份之间无法共享空闲算力,资源利用率反而下降

**vNPU 的价值在于多租户隔离与 QoS 保障**——保证某个业务不会被邻居挤占,而非提升整卡吞吐。若客户场景是单一 OCR 业务独占整卡,不建议启用算力切分。

---

## 7. 数据处理方法

### 7.1 测试工具链

| 脚本 | 作用 |
|---|---|
| `dataset_profile.py` | 扫描数据集,输出分辨率 / 通道 / 大小分布 |
| `text_density.py` | 统计每页文本行数分布 |
| `rec_width_probe.py` | 全量扫描 rec 输入宽度分布,用于确定 om 动态范围 |
| `bench.py` | 单进程压测,输出 QPS 与时延分位数（JSON） |
| `bench_stage.py` | 分段计时,对耗时做线性回归 |
| `acc_check.py` | om vs Paddle 后端字符级一致性比对 |
| `regress.sh` | 全量 104 张回归,验证模型改动后功能不退化 |
| `monitor.sh` | 后台采集 AICore / HBM / CPU / 内存,输出 CSV 与汇总 JSON |
| `run.sh` | 压测与资源采集绑定执行,自动保存配置快照 |

### 7.2 数据规范

- 所有压测结果落盘为 JSON（`results/<tag>_<pid>.json`）,不依赖终端输出
- 每轮实验同时保存 `OCR.yml` 配置快照,确保可追溯
- 资源采样 1 秒一次,汇总输出 avg / max / p50

### 7.3 统计方法

| 方法 | 应用 |
|---|---|
| 变异系数 | 判断单实例基线稳定性（0.67%,判定为稳定） |
| 最小二乘线性回归 | 耗时对文本行数建模,拆分固定开销与可变开销 |
| 分位数（P50/P90/P99） | 时延分布,避免均值掩盖长尾 |
| 扩展效率 | `聚合QPS / (单实例QPS × 实例数)`,量化并发衰减 |
| 理论上限推算与实测比对 | 显存约束模型的验证（23 vs 24、36 vs 38,吻合） |

### 7.4 可视化

6 张图表覆盖:扩展曲线、资源利用率、耗时构成、显存优化、宽度分布、时延代价。图表数据全部来自实测记录,不含插值或估算值;涉及推算的部分（vNPU）在正文中明确标注。

---

## 8. 结论

### 8.1 任务一:客户数据集摸测

1. **单实例吞吐 1.21 QPS,平均时延 825 ms,P99 1592 ms**,三次重复变异系数 0.67%。
2. **耗时构成为固定开销 399 ms + 12.50 ms × 文本行数**,rec 推理占典型页耗时的 59%。固定开销中约 300 ms 为 CPU 单线程的图片解码与前后处理,不使用 NPU。
3. **精度可接受**:om 后端与 Paddle 后端字符一致率 99.88%,最低 99.17%,零推理失败。
4. **单实例状态下 NPU 与 CPU 均严重空闲**（AICore 2.9%）,瓶颈是串行等待而非算力不足。

### 8.2 任务二:多实例吞吐与算力切分

1. **单卡最高吞吐 27.96 QPS**,在约 38 实例时达到,相对单实例提升 23 倍。
2. **瓶颈是 HBM 显存容量,不是算力**——拐点处 AICore 仅 48~54%,HBM 已达 99%。
3. **`ASCEND_OM_OUTPUTSIZE` 从 1024 降至 64,单卡吞吐提升 36.6%**,单实例性能与精度均无损失。
4. **算力切分对本负载无吞吐收益**,其价值在多租户隔离与 QoS 保障。
5. **吞吐与时延存在取舍**:38 实例时 P99 接近 4 s,若客户有时延 SLA,需在 8~16 实例区间选择工作点。

### 8.3 部署建议

| 场景 | 建议配置 | 预期表现 |
|---|---|---|
| 吞吐优先（批处理） | 单卡 36~38 实例,`OUTPUTSIZE=64` | ~28 QPS/卡,P99 约 4 s |
| 时延优先（在线服务） | 单卡 8~16 实例 | 8~15 QPS/卡,P99 < 2 s |
| 多租户隔离 | vir10_3c_32g 切分,每份 16~18 实例 | 吞吐低于物理卡,换取隔离性 |

**必须配置项**:
- `ASCEND_OM_OUTPUTSIZE=64`
- rec / det 模型转换**不得使用 onnxsim**
- om 动态 shape 范围需按客户实际数据全量统计后确定

---

## 9. 放弃或调整的工作项及原因

| 工作项 | 状态 | 原因 |
|---|---|---|
| **整机 8 卡满配测试** | 未执行 | PL 指示不做。技术上脚本已就绪,主要制约是 8 卡仅有 128 个 NUMA 亲和核,以及 288 进程所需的约 432 GB host 内存且8卡无法长时间被我独自占据 |
| **vNPU 压测数据** | 未获得 | 容器无法在 vNPU 上初始化 ACL（见 6.5.2）。时间受限,改为基于已测资源数据给出推算结论 |
| **单实例参数调优** | 放弃 | 原计划扫描 `limit_side_len`（736/960/1280）、`rec batch_size`（1/2/4/8）、KPCV 开关。发现瓶颈是显存而非算力后,该方向的边际收益显著低于显存优化,时间投入到后者 |
| **om 动态范围收窄** | 放弃 | 同上。rec 上界 3200 相对实测 max 2185 有 46% 余量,理论上收窄可减少动态档位,但相对显存优化收益有限 |
| **精度校验样本量** | 由 104 张降为 30 张 | Paddle 后端 CPU 推理耗时过长（预估 40~90 分钟）。改为均匀抽样 30 张,覆盖全部 4 个目录,一致率 99.88% 已足够支撑结论 |
| **det 模型静态 shape A/B** | 放弃 | 原计划利用数据集尺寸单一的特点做静态 vs 动态对照,预期有小幅收益。优先级低于显存优化,时间不足 |
| **按目录分组测试** | 调整为混合测试 | 数据集画像显示 4 个目录尺寸完全一致,分组无区分度。改为以文本行数为自变量建模,信息量更大 |
| **CPU 利用率采集** | 未获得有效数据 | 采集脚本的 `sed` 修改两次均因正则未匹配缩进行而静默失败,全程 `cpu_pct=0`。瓶颈判断基于 HBM 与 AICore,结论不受影响 |

---

## 10. 局限性说明

以下事项如实列出,供阅读数据时参考:

1. **样本量仅 104 张**,单实例十余秒即可跑完,测试中通过 `--loop` 循环复用样本凑够测试时长（500 张 = 104 × 10 取前 500）。**存在 page cache 效应,图片解码耗时可能被低估,数据偏乐观。** 已向 PL 反馈需要更多样本。

2. **两组爬坡测试的参数不同**,不可直接跨组比较绝对 QPS:
   - OUT=1024 组:`--limit 100 --loop 5 --warmup 20`
   - OUT=64 组:`--limit 60 --loop 3 --warmup 15`
   
   显存优化的效果结论主要依据**直接实测的每实例显存占用**（2686 → 1726 MB）与**实例数上限**（24 → ~38）,而非跨组的 QPS 数值比较。

3. **56 与 72 实例组数据残缺**,分别仅 38 与 36 个实例成功写出结果。该两行聚合值不代表标称实例数的真实吞吐。

4. **CPU 利用率全程未采集成功**,瓶颈判断基于 HBM 与 AICore 两项指标。

5. **未开启 BIOS 高性能模式**（服务器为共享资源,协调独占后未申请重启进行 BIOS 配置）。透明大页已开启。

6. **vNPU 结论为推算**,非实测。推算基于已实测的 AICore 利用率与单实例显存占用,逻辑链完整,但未经压测验证。

7. **精度校验为 30 张抽样**,非全量 104 张。抽样方式为按排序后步长均匀取样,覆盖全部 4 个目录。

8. **`ASCEND_OM_OUTPUTSIZE` 的合法上界未确定**。已知 1024 有效、1288 与 524288000 被判 invalid,但未系统测试边界值。

---

## 11. 改进建议

### 11.1 对本次测试方法的改进

| 项 | 建议 |
|---|---|
| 样本量 | 争取 500~1000 张真实样本,消除 loop 循环带来的缓存效应 |
| 测试参数一致性 | 所有对照组使用完全相同的 `--limit / --loop / --warmup`,避免出现本次两组不可比的情况 |
| 资源采集 | 采集脚本落盘后应立即用一次手动执行验证输出非零,而非假设 `sed` 修改成功 |
| 回归前置 | 每次模型变更后先跑 `regress.sh` 全量 104 张,再跑性能测试。本次多次因"单张图通过"而误判,浪费了大量时间 |
| 底层日志 | 遇到 ACL 错误码应尽早开启 `ASCEND_GLOBAL_LOG_LEVEL=1` 查 plog。本次在多个错误假设上耗费数小时,而 plog 一次就定位到根因 |

### 11.2 对被测系统的改进建议

| 方向 | 具体建议 | 预期收益 |
|---|---|---|
| **CPU 前后处理** | 固定开销 399 ms 中约 300 ms 为 CPU 单线程工作。可考虑:PNG 解码使用 DVPP 硬件解码;DB 后处理算子化 | 单实例 QPS 有较大提升空间 |
| **动态档位** | rec 输入宽度跨度 34~2185,档位切换开销显著（R² 仅 0.681）。可考虑按宽度分桶,使用 2~3 个不同范围的 om | 减少档位切换,提升 rec 效率 |
| **显存** | 已通过 OUTPUTSIZE 优化。进一步可探索多进程共享模型权重 | 实例上限可再提升 |
| **rec batch** | 本次因排错需要固定为 batch=1。恢复动态 batch 后需重新评估——但需注意文本宽度分布离散,batch 内 padding 浪费可能抵消收益 | 待验证 |
| **参数调优** | `limit_side_len` / KPCV 开关的敏感性本次未测,建议后续补充 | 未知,预估 10~30% |

### 11.3 需要向上游反馈的问题

1. **昇腾社区《PaddleOCR优化参考实践》3.4 节的 `onnxsim` 建议在动态 shape 场景下会导致功能性错误**,建议文档中增加说明或移除该步骤。
2. **`ASCEND_OM_OUTPUTSIZE` 超出合法范围时静默回退到默认值**,建议改为显式报错或至少提高日志级别,避免误导排错。
3. **Ascend Docker Runtime 的 vNPU 设备标识格式缺少文档说明**,建议补充。

### 11.4 后续可开展的工作

- 整机 8 卡满配测试（脚本已就绪,需协调 host 内存与 NUMA 分配策略）
- vNPU 压测（需先解决容器 ACL 初始化问题）
- 服务化模式（HTTP）压测——客户实际大概率是 HTTP 调用,离线脚本数据不能直接套用
- 长稳测试——本次单轮最长约 7 分钟,未验证长时间运行的稳定性与内存泄漏

---

## 12. 附录

### 12.1 完整数据表

**单实例基线（三次重复）**

| tag | n | wall_s | QPS | avg_ms | p50 | p90 | p99 | max |
|---|---|---|---|---|---|---|---|---|
| base_r1 | 500 | 411.298 | 1.216 | 822.59 | 832.61 | 1201.22 | 1581.14 | 2805.03 |
| base_r2 | 500 | 415.721 | 1.203 | 831.44 | 838.51 | 1219.43 | 1634.88 | 3105.94 |
| base_final | 500 | 410.539 | 1.218 | 821.07 | 826.52 | 1197.30 | 1559.58 | 2779.13 |

**显存对照**

| tag | OUTPUTSIZE | 进程 HBM (MB) | QPS | avg_ms | p99 |
|---|---|---|---|---|---|
| m1024 | 1024 | 2686 | 1.211 | 825.89 | 1524.02 |
| m256 | 256 | 1918 | 1.211 | 826.01 | 1501.39 |
| m64 | 64 | 1726 | 1.208 | 827.86 | 1541.12 |

**NUMA 对照**

| tag | CPU 范围 | QPS | avg_ms | p99 |
|---|---|---|---|---|
| numa_wrong | 1-4 | 1.202 | 831.71 | 1523.27 |
| numa_right | 192-195 | 1.215 | 823.23 | 1521.34 |

**爬坡（OUT=1024）**

| 实例数 | 聚合 QPS | 单实例 | 效率 | AICore avg/max | HBM avg/max |
|---|---|---|---|---|---|
| 1 | 1.21 | 1.207 | 100% | 2.9 / 14.0 | 8.8 / 9 |
| 2 | 2.31 | 1.156 | 95.6% | — | — |
| 4 | 4.45 | 1.111 | 92.1% | — | — |
| 8 | 8.31 | 1.031 | 86.0% | — | — |
| 16 | 15.13 | 0.945 | 78.3% | 41.8 / 96.0 | 52.0 / 70 |
| 24 | 20.47 | 0.853 | 70.7% | 53.6 / 100.0 | 75.4 / 99 |
| 32 | 20.02 | 0.834 | 69.1% | 52.4 / 100.0 | 83.6 / 99 |

**爬坡（OUT=64）**

| 启动 | 存活 | 聚合 QPS | 单实例 | avg_ms | P99max | AICore avg | HBM avg/max |
|---|---|---|---|---|---|---|---|
| 24 | 24 | 18.75 | 0.781 | 1281 | 3240 | 52.5 | 51.4 / 66 |
| 40 | 40 | 24.47 | 0.612 | 1636 | 4610 | 48.2 | 81.3 / 99 |
| 56 | 38 | 27.96 | 0.736 | 1371 | 3869 | 48.2 | 73.7 / 99 |
| 72 | 36 | 23.65 | 0.657 | 1541 | 4093 | 48.1 | 69.4 / 99 |

**精度校验**

| 项 | 值 |
|---|---|
| 样本 | 30 |
| 完全一致 | 21 |
| 平均字符一致率 | 0.998831 |
| 最低一致率 | 0.991667 |
| 推理失败 | 0 |

差异样本一致率:0.9944、0.9967、0.9969、0.9923、0.9987、0.9991、0.9917、0.9979、0.9973

### 12.2 最终产线配置

```yaml
pipeline_name: OCR
text_type: general
use_doc_preprocessor: False
use_textline_orientation: False
hpi_config:
  auto_config: False
  backend: om
  device_type: npu
SubPipelines:
  DocPreprocessor:
    pipeline_name: doc_preprocessor
    use_doc_orientation_classify: False
    use_doc_unwarping: False
    SubModules:
      DocOrientationClassify:
        module_name: doc_text_orientation
        model_name: PP-LCNet_x1_0_doc_ori
        model_dir: null
      DocUnwarping:
        module_name: image_unwarping
        model_name: UVDoc
        model_dir: null
SubModules:
  TextDetection:
    module_name: text_detection
    model_name: PP-OCRv5_server_det
    model_dir: /data/models/PP-OCRv5_server_det_infer
    limit_type: max
    limit_side_len: 960
  TextLineOrientation:
    module_name: textline_orientation
    model_name: PP-LCNet_x0_25_textline_ori
    model_dir: null
  TextRecognition:
    module_name: text_recognition
    model_name: PP-OCRv5_server_rec
    model_dir: /data/models/PP-OCRv5_server_rec_infer
    batch_size: 4
```

### 12.3 模型转换命令

```bash
export SOC=Ascend910B4-1

# det —— 注意不使用 onnxsim
cd /data/models/PP-OCRv5_server_det_infer/
atc --framework=5 --model=inference.onnx --output=det_final \
    --input_format=ND --input_shape="x:1,3,320~1408,320~1408" \
    --log=error --precision_mode_v2=origin --soc_version=$SOC
cp det_final_linux_aarch64.om inference.om

# rec —— 注意不使用 onnxsim
cd /data/models/PP-OCRv5_server_rec_infer/
atc --framework=5 --model=inference.onnx --output=rec_final \
    --input_format=ND --input_shape="x:1,3,48,8~3200" \
    --log=error --soc_version=$SOC
cp rec_final_linux_aarch64.om inference.om
```

### 12.4 术语与错误码速查

| 错误码 | 含义 | 本次遇到的场景 |
|---|---|---|
| `-8005` | DCMI 初始化失败 | 容器挂载不全 / 用户不在 HwHiAiUser 组 |
| `-8020` | 设备被占用 | （未遇到,与 -8005 区分用） |
| `500000` | ACL 初始化失败 | vNPU 容器未完成设备绑定 |
| `500002` | 模型执行失败,参数无效 | shape 超出 om 范围 / onnxsim 导致的 broadcast 失败 |
| `100000` | 内部错误 | 输出内存不足（`Model infer failed with dynamic shape`） |
| `107000` | 参数越界 | 输入宽度超出 om 上界 |
| `127`（shell） | 命令未找到 | Dockerfile 通配符未匹配到文件 |

### 12.5 图表清单

| 图 | 文件 | 内容 |
|---|---|---|
| 图 1 | `figs/fig1_scaling.png` | 单卡多实例吞吐爬坡（两组配置对比） |
| 图 2 | `figs/fig2_resource.png` | 资源利用率:HBM 先于 AICore 饱和 |
| 图 3 | `figs/fig3_stage.png` | 耗时构成:线性回归拆分固定开销与 rec 开销 |
| 图 4 | `figs/fig4_memory.png` | 显存优化效果与实例上限 |
| 图 5 | `figs/fig5_recwidth.png` | rec 文本行宽度分布与两次 om 范围错误 |
| 图 6 | `figs/fig6_latency.png` | 并发提升的时延代价 |

---

*本报告数据均来自实测记录,推算部分已在正文明确标注。原始 JSON 与 CSV 保存于 `/data/bench/results/`。*



# PaddleOCR 昇腾平台性能摸测报告\(V2 · 双机验证版\)

**测试对象**:PP\-OCRv5\_server\(det + rec\)on Ascend 910B4\-1

**测试范围**:单卡单实例基线 + 单卡多实例吞吐爬坡

**验证方式**:两台独立服务器交叉验证

**报告日期**:2026\-09\-12

**版本说明**:本版取代 09\-10 初版。初版数据（09\_08\-09\_09）在环境受前序作业影响的状态下采集,已作废,不再引用。

***

## 摘要

| 指标                 | 服务器 A              | 服务器 B                      | 结论                     |
| -------------------- | --------------------- | ----------------------------- | ------------------------ |
| 单实例吞吐           | 3.40 QPS              | **3.12 QPS**\(6 次,CV 1.24%\) | 两机差 7.6%              |
| 单实例平均时延       | 294 ms                | 320 ms                        | —                        |
| **单卡峰值吞吐**     | **41.83 QPS**         | **41.72 QPS**                 | **两机差 0.3%**          |
| 峰值对应实例数       | 38                    | 44                            | —                        |
| 单卡实例上限         | ~42                   | ~41                           | 受 HBM 容量限制          |
| **推荐运行点**       | **24 实例**           | **24 实例**                   | 92% 峰值吞吐,P99 ≈ 1.5 s |
| **性能瓶颈**         | **AICore 算力**       | 同左                          | 16 实例即达 89.7%        |
| 精度\(om vs Paddle\) | 平均字符一致率 99.88% | —                             | 零推理失败               |

**核心发现:两台服务器单实例性能相差 7.6%,但峰值吞吐几乎完全一致\(差 0.3%\)。**

这说明单卡吞吐上限由 NPU 算力决定,与宿主机的 CPU、内存等差异基本无关。对客户的容量规划而言,**单卡 ≈ 41.8 QPS 是一个可跨机器复现的稳定结论**。

***

## 目录

1. [测试环境](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#1-测试环境)

2. [数据集特征](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#2-数据集特征)

3. [单实例基线](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#3-单实例基线)

4. [单页耗时构成](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#4-单页耗时构成)

5. [多实例吞吐爬坡](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#5-多实例吞吐爬坡)

6. [瓶颈定位](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#6-瓶颈定位)

7. [吞吐与时延的权衡](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#7-吞吐与时延的权衡)

8. [精度验证](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#8-精度验证)

9. [测量方法学校验](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#9-测量方法学校验)

10. [结论与部署建议](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#10-结论与部署建议)

11. [完整复现教程](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#11-完整复现教程)

12. [局限性说明](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#12-局限性说明)

13. [附录](https://claude.ai/chat/f22236e5-0592-4576-854b-21a40a8595a4#13-附录)

***

## 1. 测试环境

### 1.1 硬件

| 项              | 服务器 A                               | 服务器 B            |
| --------------- | -------------------------------------- | ------------------- |
| NPU             | 8 × Ascend 910B4\-1                    | 8 × Ascend 910B4\-1 |
| 单卡 HBM        | 65536 MB                               | 65536 MB            |
| CPU             | Kunpeng\-920 7265,256 核 @3.0GHz  \* 4 | 同型号 \* 3         |
| OS              | openEuler 24.03 LTS SP3,kernel 6.6.0   | 同                  |
| 驱动 / npu\-smi | 26.0.rc1                               | 26.0.rc1            |

两机硬件规格一致,**测试中仅使用卡 0**。

### 1.2 系统调优\(两机一致\)

```bash
# 透明大页
cat /sys/kernel/mm/transparent_hugepage/enabled   # [always]
# CPU 调频策略
cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor   # performance
```

两项均已确认一致,这是双机对照成立的前提。

### 1.3 软件栈

| 组件              | 版本                        |
| ----------------- | --------------------------- |
| CANN Toolkit      | 8.5.0                       |
| PaddleX           | 3.4.2\(含动态 shape patch\) |
| ultra\-infer\-npu | 1.2.0                       |
| OpenCV            | 4.10.0 + 鲲鹏 KPCV          |
| Python            | 3.11                        |
| 基础镜像          | openEuler 24.03 LTS SP3     |

服务器 B 使用与 A 完全相同的镜像\(`docker save` 导出后导入\),om 模型文件 md5 一致。

### 1.4 模型与关键参数

| 项                     | 值                                                   |
| ---------------------- | ---------------------------------------------------- |
| det om                 | `x:1,3,320~1408,320~1408`,`precision_mode_v2=origin` |
| rec om                 | `x:1,3,48,8~3200`                                    |
| **onnxsim**            | **不使用**\(使用会导致约 35% 图片推理失败,见 12.4\)  |
| `ASCEND_OM_OUTPUTSIZE` | 64\(MB\)                                             |
| `limit_side_len`       | 960                                                  |
| rec `batch_size`       | 4                                                    |
| soc\_version           | `Ascend910B4-1`                                      |

***

## 2. 数据集特征

客户提供 4 个目录、**104 张 PNG**,内容为招投标文档扫描件。

| 属性         | 值                                     |
| ------------ | -------------------------------------- |
| 尺寸         | 仅三种:1449×2048、2048×1449、1448×2048 |
| 通道         | 全部 RGB                               |
| 平均文件大小 | 830.7 KB,全集 84.4 MB                  |
| 分辨率       | A4 比例,长边 2048 ≈ 175 DPI            |
| 损坏文件     | 0                                      |

### 关键洞察:性能变量是文本密度而非分辨率

尺寸高度统一,**决定单页耗时的是每页文本行数**——det 检出多少框,rec 就执行多少次推理。

| 指标                                | 值                 |
| ----------------------------------- | ------------------ |
| 每页文本行数 min / 中位 / P90 / max | 13 / 41 / 69 / 209 |
| 均值                                | 44.8               |
| **全集总文本行数**                  | **4661**           |

> **总文本行数 4661 是本次测试的完整性校验值。** 任何环境下重跑,该值必须一致,否则说明检测行为发生变化,性能数据失去可比性。服务器 B 已核对通过。

### rec 输入宽度分布

全量扫描 4762 个文本框\(高度归一化至 48\):

| min  | P1   | 中位 | P99  | P99.9 | max      |
| ---- | ---- | ---- | ---- | ----- | -------- |
| 3    | 34   | 185  | 1553 | 1930  | **2185** |

宽度跨度超过 60 倍,48% 的文本框宽度低于 160。这一分布决定了 om 动态 shape 范围必须设为 `8~3200`\(见 11.5\)。

***

## 3. 单实例基线

### 3.1 服务器 B 六次重复

测试参数:`--limit 500 --loop 10 --warmup 30`,即预热 30 张后连续计时 500 张。

| 轮次         | QPS        | avg \(ms\) | P50    | P90    | P99        | max    |
| ------------ | ---------- | ---------- | ------ | ------ | ---------- | ------ |
| r1           | 3.191      | 313.34     | 320.54 | 439.58 | 624.59     | 698.36 |
| r2           | 3.091      | 323.46     | 332.94 | 449.37 | 691.53     | 739.24 |
| r3           | 3.133      | 319.13     | 326.89 | 444.27 | 632.08     | 734.93 |
| r4           | 3.084      | 324.22     | 331.71 | 453.16 | 631.44     | 862.60 |
| r5           | 3.108      | 321.71     | 332.08 | 448.09 | 627.84     | 783.22 |
| r6           | 3.128      | 319.68     | 326.64 | 447.11 | 627.66     | 740.54 |
| **均值**     | **3.1225** | **320.26** | 328.47 | 446.93 | **639.19** | 759.82 |
| 标准差       | 0.0388     | —          | —      | —      | —          | —      |
| **变异系数** | **1.24%**  | —          | —      | —      | —          | —      |

![](https://clouddocs.huawei.com/koopage/v1/app/api/documents/doc/preview/63239fc3-1eb7-4cf5-a0ce-ff049042705d?document_id=565d5366-fc39-4c21-936d-946c933b2c5b "")

变异系数 1.24%,测试稳定性良好,六次重复足以支撑结论。

### 3.2 两机对照

| 指标 | 服务器 A | 服务器 B | 差异      |
| ---- | -------- | -------- | --------- |
| QPS  | 3.401    | 3.143    | **−7.6%** |
| avg  | 294 ms   | 318 ms   | +8.2%     |
| P99  | 584 ms   | 706 ms   | +20.9%    |

> 上表取两机爬坡实验中 1 实例档的数据,测试脚本与绑核方式完全相同,可直接比较。

两机硬件规格相同但单实例性能差 7.6%。可能来源包括宿主机后台负载、内存分配状态、单核实际运行频率等。**该差异在多实例场景下会消失\(见第 5 节\),因此不影响容量规划结论。**

***

## 4. 单页耗时构成

对全量 104 张单独计时,以文本行数为自变量做最小二乘回归:

$$ \text{单页耗时(ms)} = 160.7 + 3.10 \times \text{文本行数} \qquad R^2 = 0.501 $$

![](https://clouddocs.huawei.com/koopage/v1/app/api/documents/doc/preview/e2fde55a-c5d4-4932-9334-9dccad6b5041?document_id=565d5366-fc39-4c21-936d-946c933b2c5b "")

| 组成                                              | 典型页\(45 行\) | 占比 |
| ------------------------------------------------- | --------------- | ---- |
| 固定开销:PNG 解码 + resize + det 推理 + DB 后处理 | 161 ms          | 54%  |
| 可变开销:rec 推理 + 文本框裁剪                    | 139 ms          | 46%  |

### 解读

**固定开销 161 ms 中 det 推理仅占少部分,其余为 CPU 单线程工作**——830 KB PNG 解码、缩放、DB 轮廓提取、以及 45 个文本框的裁剪与透视变换。这部分不使用 NPU。

**R² = 0.501 说明文本行数只能解释一半的耗时波动。** 剩余方差来自文本行的**宽度**:同样 45 行,全是短标签与全是长句子的 rec 计算量差异很大,而行数统计没有体现宽度信息。

***

## 5. 多实例吞吐爬坡

### 5.1 完整数据

测试方法:同一张卡\(卡 0\)上启动 N 个独立进程,各自绑定互不重叠的 CPU 核区间,同时压测。聚合吞吐为各进程 QPS 之和。

| 实例数 | 服务器 A | 服务器 B  | 差异  | B:存活    | B:单实例 | B:扩展效率 | B:avg | B:P99max |
| ------ | -------- | --------- | ----- | --------- | -------- | ---------- | ----- | -------- |
| 1      | 3.40     | 3.14      | −7.6% | 1/1       | 3.143    | 100%       | 318   | 706      |
| 4      | 13.14    | 11.97     | −8.9% | 4/4       | 2.993    | 95.2%      | 334   | 681      |
| 8      | 22.21    | 21.42     | −3.6% | 8/8       | 2.677    | 85.2%      | 374   | 768      |
| 12     | —        | 28.41     | —     | 12/12     | 2.368    | 75.3%      | 423   | 1005     |
| 16     | 33.37    | 33.67     | +0.9% | 16/16     | 2.104    | 67.0%      | 476   | 1059     |
| 20     | —        | 36.74     | —     | 20/20     | 1.837    | 58.4%      | 545   | 1424     |
| **24** | 38.10    | **38.54** | +1.2% | 24/24     | 1.606    | 51.1%      | 623   | 1497     |
| 32     | 39.22    | 39.42     | +0.5% | 32/32     | 1.232    | 39.2%      | 812   | 1915     |
| 38     | 41.83    | 41.33     | −1.2% | 38/38     | 1.088    | 34.6%      | 920   | 2247     |
| 44     | 41.70    | **41.72** | +0.1% | **41/44** | 1.017    | 30.2%      | 985   | 2949     |

![](https://clouddocs.huawei.com/koopage/v1/app/api/documents/doc/preview/03f8b3d0-bf06-4c9d-9cb8-d687fa16f6a1?document_id=565d5366-fc39-4c21-936d-946c933b2c5b "")

### 5.2 两机在高并发下高度收敛

这是本次双机验证最重要的发现:

| 实例数        | 两机差异            |
| ------------- | ------------------- |
| 1             | −7.6%               |
| 4             | −8.9%               |
| 8             | −3.6%               |
| **16 及以上** | **均在 ±1.2% 以内** |

**低并发时两机差异明显,高并发时收敛到 1% 以内。**

原因是:低并发下性能受宿主机 CPU 侧的解码与后处理效率影响,两机存在差异;高并发下 NPU 算力成为瓶颈,而两机 NPU 规格完全相同,因此峰值一致。

**这一现象反过来验证了瓶颈判断的正确性,也说明"单卡峰值 ≈ 41.8 QPS"是可跨机器复现的稳定结论。**

### 5.3 扩展效率

![](https://clouddocs.huawei.com/koopage/v1/app/api/documents/doc/preview/c15d2022-ad89-46fa-b14c-0cb92427d557?document_id=565d5366-fc39-4c21-936d-946c933b2c5b "")

$$ \text{扩展效率} = \frac{\text{聚合吞吐}}{\text{单实例吞吐} \times \text{实例数}} $$

| 实例数 | 4     | 8     | 12    | 16    | 20    | 24    | 32    | 44    |
| ------ | ----- | ----- | ----- | ----- | ----- | ----- | ----- | ----- |
| 效率   | 95.2% | 85.2% | 75.3% | 67.0% | 58.4% | 51.1% | 39.2% | 30.2% |

两机曲线几乎重合。8 实例跌破 85%,24 实例仅剩一半。

### 5.4 边际收益递减

![](https://clouddocs.huawei.com/koopage/v1/app/api/documents/doc/preview/c2854a42-efe9-427e-bfb0-44ee4e422bda?document_id=565d5366-fc39-4c21-936d-946c933b2c5b "")

| 区间      | 每新增 1 实例带来的 QPS |
| --------- | ----------------------- |
| 1→4       | 2.94                    |
| 4→8       | 2.36                    |
| 8→12      | 1.75                    |
| 12→16     | 1.32                    |
| **16→20** | **0.77**                |
| 20→24     | 0.45                    |
| 24→32     | 0.11                    |
| 32→38     | 0.32                    |
| 38→44     | 0.07                    |

**20 实例之后边际收益跌破 0.5 QPS/实例,24 实例之后基本枯竭。**

### 5.5 实例数上限

44 实例档,服务器 A 存活 42、服务器 B 存活 41,超出部分因显存不足在初始化阶段失败。

按每实例约 1500 MB、可用 HBM 约 62 GB 推算,理论上限约 41,与实测吻合。

**但该上限已无实际意义**——吞吐在 24 实例即达峰值的 92%,继续增加实例只增时延不增吞吐。

***

## 6. 瓶颈定位

![](https://clouddocs.huawei.com/koopage/v1/app/api/documents/doc/preview/55c833e8-175c-41ec-ae0b-e87862ce07e4?document_id=565d5366-fc39-4c21-936d-946c933b2c5b "")

服务器 A 的资源采样数据\(1 秒一次,取整轮均值\):

| 实例数        | 1    | 4    | 8    | **16**   | 24   | 32   | 38   | 44   |
| ------------- | ---- | ---- | ---- | -------- | ---- | ---- | ---- | ---- |
| **AICore %**  | 8.8  | 39.2 | 73.6 | **89.7** | 90.2 | 92.8 | 92.8 | 86.0 |
| HBM %         | 6.8  | 14.0 | 23.9 | 43.0     | 60.9 | 82.2 | 88.5 | 88.4 |
| CPU\(核,p50\) | 1.6  | 6.1  | 7.6  | 13.4     | 17.2 | 18.8 | 20.1 | 24.0 |

### 结论:瓶颈是 AICore 算力

| 资源       | 饱和位置                        | 判定                          |
| ---------- | ------------------------------- | ----------------------------- |
| **AICore** | **16 实例达 89.7%,p50 已 100%** | ✅ **瓶颈**                    |
| HBM        | 38 实例才达 88.5%               | ❌ 仅限制实例数上限,不限制吞吐 |
| CPU        | 峰值 24 核 / 256 核             | ❌ 完全空闲                    |

**16 实例时 AICore 已达 89.7%,此后从 16 增至 38 实例\(+137%\),吞吐仅涨 24%。** 多出来的实例大部分时间在排队等待 NPU。

CPU 侧尤其明确:峰值仅用 24 核,占 256 核的 9.4%;且每实例 CPU 占用随并发从 1.6 核降至 0.55 核,说明实例大部分时间在等 NPU 而非在算——与 AICore 饱和互相印证。

***

## 7. 吞吐与时延的权衡

![](https://clouddocs.huawei.com/koopage/v1/app/api/documents/doc/preview/06d8bb03-c4fd-47a6-9ee0-2df950b2ce98?document_id=565d5366-fc39-4c21-936d-946c933b2c5b "")

| 实例数   | 吞吐      | 占峰值  | avg        | **P99**     |
| -------- | --------- | ------- | ---------- | ----------- |
| 8        | 21.42     | 51%     | 374 ms     | 768 ms      |
| 16       | 33.67     | 81%     | 476 ms     | 1059 ms     |
| 20       | 36.74     | 88%     | 545 ms     | 1424 ms     |
| **24** ★ | **38.54** | **92%** | **623 ms** | **1497 ms** |
| 32       | 39.42     | 94%     | 812 ms     | 1915 ms     |
| 38       | 41.33     | 99%     | 920 ms     | 2247 ms     |
| 44       | 41.72     | 100%    | 985 ms     | 2949 ms     |

**从 24 实例增至 44 实例:吞吐仅 +8.3%,而 P99 从 1.5 s 涨到 2.9 s\(+97%\)。**

推荐运行点见第 10 节。

***

## 8. 精度验证

以 Paddle 后端\(CPU,`use_hpip=False`\)输出为参考答案,与 om 后端做字符级一致性比对。

| 项                 | 值                                        |
| ------------------ | ----------------------------------------- |
| 比对样本           | 30 张\(104 张全集均匀抽样,覆盖 4 个目录\) |
| 完全一致           | 21 / 30 \(70.0%\)                         |
| **平均字符一致率** | **99.88%**                                |
| 最低一致率         | 99.17%                                    |
| 推理失败           | 0                                         |

一致率计算方式:`difflib.SequenceMatcher.ratio() = 2M/T`,M 为匹配字符数,T 为两串长度之和。

差异最大的 5 个样本一致率为 99.17%、99.23%、99.44%、99.67%、99.69%,差异均为个别字符级别,**无整行漏检或错行**。

**结论:om 后端精度可用于生产。**

***

## 9. 测量方法学校验

为确保数据可信,对两个可能的系统性偏差做了专项验证。

### 9.1 page cache 影响

测试设计:完整预热 104 张\(消除动态 shape 首次分档影响\)→ `drop_caches` → 冷 IO 整轮 → 热 IO 整轮,重复 3 轮取中位数。

| 项              | 值                                      |
| --------------- | --------------------------------------- |
| 冷 IO 整轮      | 29.402 s\(每张 282.71 ms\)              |
| 热 IO 整轮      | 29.301 s\(每张 281.74 ms\)              |
| **差异**        | **+0.34%**\(每张 0.97 ms\)              |
| 纯文件读取冷/热 | 81.6 ms / 18.4 ms,差 63.2 ms\(84.4 MB\) |
| 纯 IO 差占整轮  | 0.21%                                   |

端到端实测 0.34% 与纯 IO 理论上限 0.21% 吻合,交叉验证通过。

**结论:page cache 影响可忽略,使用 ****`--loop`**** 循环复用样本的测试数据无需修正。**

> 补充:冷读速率 84.4 MB / 81.6 ms = 1.03 GB/s\(NVMe 正常值\),热读 4.59 GB/s\(内存带宽\)。倍数虽达 4.4 倍,但绝对值仅占单张耗时 282 ms 的 0.3%。

### 9.2 NUMA 绑核影响

| 绑核策略                           | QPS   |
| ---------------------------------- | ----- |
| 跨 NUMA\(核 1\-4\)                 | 1.202 |
| 亲和 NUMA\(核 192\-195,卡 0 亲和\) | 1.215 |
| 差异                               | +1.1% |

单实例下差异在噪声范围内。原因是 CPU 与 AICore 利用率均极低,内存带宽无压力,跨 NUMA 的额外延迟被空闲时间掩盖。

多实例测试统一采用宽绑核\(核区间平分,不严格按 NUMA 亲和\),因为卡 0 亲和的 NUMA 节点仅 32 核,严格亲和会导致高并发下每实例分不到 1 个核。

***

## 10. 结论与部署建议

### 10.1 核心结论

1. **单卡峰值吞吐约 41.8 QPS**,两台独立服务器实测差异 0.3%,结论稳定可复现。

2. **瓶颈是 AICore 算力**,16 实例即达 89.7%;HBM 仅限制实例数上限\(~41\),CPU 完全空闲\(峰值 24/256 核\)。

3. **单实例吞吐 3.1~3.4 QPS**,平均时延 294~320 ms,变异系数 1.24%。

4. **精度可用**:om 后端与 Paddle 后端字符一致率 99.88%,零推理失败。

5. **边际收益在 20 实例后跌破 0.5 QPS/实例**,24 实例后基本枯竭。

### 10.2 推荐运行点

| 场景                 | 实例数/卡 | 吞吐          | P99       | 说明                          |
| -------------------- | --------- | ------------- | --------- | ----------------------------- |
| **在线服务\(推荐\)** | **24**    | **38.5 QPS**  | **1.5 s** | 92% 峰值吞吐,时延可控         |
| 时延敏感             | 16        | 33.7 QPS      | 1.06 s    | 81% 吞吐,P99 控制在 1 s 出头  |
| 纯批处理             | 38~41     | 41.3~41.7 QPS | 2.2~2.9 s | 追求极限吞吐,不关心单请求时延 |
| 低延迟优先           | 8         | 21.4 QPS      | 0.77 s    | 51% 吞吐,P99 &lt; 800 ms      |

**默认推荐 24 实例。** 相比 44 实例只损失 8% 吞吐,却把 P99 从 2.9 s 降到 1.5 s。

### 10.3 容量估算

按单卡 38.5 QPS\(24 实例\)计算:

| 目标吞吐 | 需要卡数 |
| -------- | -------- |
| 100 QPS  | 3        |
| 500 QPS  | 13       |
| 1000 QPS | 26       |

> 该估算假设多卡线性扩展。本次未测整机多卡,实际部署会因内存带宽与 PCIe 存在折损,建议预留 10~20% 余量。

### 10.4 必须遵守的配置项

| 项                         | 值            | 原因                               |
| -------------------------- | ------------- | ---------------------------------- |
| **模型转换不使用 onnxsim** | —             | 会导致约 35% 图片推理失败          |
| `ASCEND_OM_OUTPUTSIZE`     | 64            | 该值为预分配,直接决定单实例显存    |
| rec om 范围                | `8~3200`      | 必须覆盖实际数据的宽度分布         |
| det om 范围                | `320~1408`    | 覆盖 `limit_side_len` 缩放后的尺寸 |
| 透明大页                   | `always`      | —                                  |
| CPU governor               | `performance` | —                                  |

***

## 11. 完整复现教程

本节提供从零开始的完整步骤,客户可据此在自己的环境中复现全部结论。

### 11.1 硬件与系统前置条件

| 项   | 要求                                                |
| ---- | --------------------------------------------------- |
| NPU  | Ascend 910B 系列\(本测试为 910B4\-1\)               |
| 驱动 | ≥ 25.0.0\(本测试 26.0.rc1\)                         |
| OS   | openEuler 24.03 LTS SP3 / Ubuntu 等昇腾支持的发行版 |
| 磁盘 | ≥ 100 GB 可用\(镜像 + 模型\)                        |
| 内存 | ≥ 64 GB\(24 实例约需 40 GB host 内存\)              |

### 11.2 环境准备

```bash
# 1) 用户权限
sudo usermod -aG docker,HwHiAiUser $(whoami)
# 重新登录生效

# 2) 验证 NPU 可见
npu-smi info
# 报 -8005 时:检查是否在 HwHiAiUser 组、驱动模块是否加载

# 3) 系统调优
echo always | sudo tee /sys/kernel/mm/transparent_hugepage/enabled
echo always | sudo tee /sys/kernel/mm/transparent_hugepage/defrag
for c in /sys/devices/system/cpu/cpu*/cpufreq/scaling_governor; do
  echo performance | sudo tee $c > /dev/null
done

# 4) Docker 数据目录挪到大盘
sudo tee /etc/docker/daemon.json <<'EOF'
{ "data-root": "/data/docker", "default-shm-size": "8G" }
EOF
sudo systemctl daemon-reload && sudo systemctl restart docker

# 5) 安装 Ascend Docker Runtime（从昇腾社区下载 .run 包）
sudo ./Ascend-docker-runtime_*.run --install
sudo systemctl restart docker
docker info | grep -i runtime
```

### 11.3 构建推理镜像

依据昇腾社区《PaddleOCR优化参考实践》构建,需要 6 个组件:

| \#   | 组件                      | 版本                |
| ---- | ------------------------- | ------------------- |
| 1    | openEuler 基础镜像        | 24.03\-LTS\-SP3     |
| 2    | PaddleX 源码              | 3.4.2               |
| 3    | CANN Toolkit              | 8.5.0               |
| 4    | CANN 算子包               | 8.5.0\(按芯片选择\) |
| 5    | OpenCV 源码\(鲲鹏可选\)   | 4.10.0              |
| 6    | BoostKit KPCV\(鲲鹏可选\) | 1.3.0               |

关键步骤:

```bash
mkdir -p /data/build && cd /data/build

# 下载时必须改名以匹配 Dockerfile 通配符
wget -O PaddleX-3.4.2.tar.gz \
  https://codeload.github.com/PaddlePaddle/PaddleX/tar.gz/refs/tags/v3.4.2
wget -O opencv-4.10.0.zip \
  https://codeload.github.com/opencv/opencv/zip/refs/tags/4.10.0
# CANN 包从昇腾社区下载

# 加载基础镜像并确认 tag 与 Dockerfile 的 FROM 一致
docker load -i openEuler-docker.aarch64.tar.xz
docker images
docker tag <实际名> openeuler-24.03-lts-sp3:latest

# paddlex.patch 使 om 后端支持动态 shape，必需
# 落盘后先验证可用
mkdir -p /tmp/pt && tar -xf PaddleX-3.4.2.tar.gz -C /tmp/pt
cd /tmp/pt/PaddleX-3.4.2 && git apply --check --verbose /data/build/paddlex.patch

# 构建（1.5~3 小时，建议 tmux）
cd /data/build
docker build -t paddlex . 2>&1 | tee build.log

# 立即备份
docker save paddlex:latest | gzip > /data/paddlex_image.tar.gz
```

> **paddlex.patch 的作用**:PaddleX 自带的 om 后端仅支持静态 shape,该 patch 使其能根据 om 模型自动走动态 shape 分支,并新增 `ASCEND_OM_OUTPUTSIZE` 环境变量控制输出内存预分配。补丁内容见官方文档 3.3.2.1 节。

### 11.4 启动容器

```bash
sudo docker run -itd -u root --name=paddlex --net=host --ipc=host \
  -e ASCEND_VISIBLE_DEVICES=0 \
  -e PADDLE_PDX_DISABLE_MODEL_SOURCE_CHECK=True \
  -e ASCEND_OM_OUTPUTSIZE=64 \
  -e SOC=Ascend910B4-1 \
  -v /data:/data --shm-size=32g \
  paddlex /bin/bash

sudo docker exec -it paddlex bash
```

> 环境变量写在 `docker run` 而非 `.bashrc`——后者在非交互式 shell 中不会加载,曾导致 ATC 因 `$SOC` 为空而静默失败。

容器自检:

```bash
npu-smi info | head -12
atc --help | head -2
python -c "import paddlex, ultra_infer; print(paddlex.__version__)"

# 确认 patch 已编译进 so
python - <<'PY'
import ultra_infer, os, subprocess, glob
d = os.path.dirname(ultra_infer.__file__)
for l in glob.glob(os.path.join(d, "**", "*.so*"), recursive=True):
    if "ASCEND_OM_OUTPUTSIZE" in subprocess.run(
            ["strings", l], capture_output=True, text=True).stdout:
        print("patch 生效 ✓", l)
PY
```

### 11.5 模型准备

```bash
# 1) 下载模型
mkdir -p /data/models && cd /data/models
pip install modelscope
modelscope download --model PaddlePaddle/PP-OCRv5_server_det --local_dir ./PP-OCRv5_server_det_infer
modelscope download --model PaddlePaddle/PP-OCRv5_server_rec --local_dir ./PP-OCRv5_server_rec_infer

# 2) 导出 onnx
python -m paddlex --paddle2onnx \
  --paddle_model_dir ./PP-OCRv5_server_det_infer/ --onnx_model_dir ./PP-OCRv5_server_det_infer/
python -m paddlex --paddle2onnx \
  --paddle_model_dir ./PP-OCRv5_server_rec_infer/ --onnx_model_dir ./PP-OCRv5_server_rec_infer/

# 3) 确认 soc_version（不同芯片写法不同，先用静态 shape 快速试）
atc --framework=5 --model=./PP-OCRv5_server_det_infer/inference.onnx \
    --output=/tmp/t --input_format=ND --input_shape="x:1,3,640,640" \
    --log=error --soc_version=Ascend910B4-1 && echo "OK"
rm -f /tmp/t.om
```

#### 用真实数据确定动态 shape 范围

**这一步不能省,也不能抽样。** 官方文档给的通用范围会导致失败。

```bash
# 全量扫描 rec 输入宽度分布
python /data/bench/rec_width_probe.py
```

根据输出的 min / max 设定范围,留 20~50% 余量。本数据集实测 min=3、max=2185,故采用 `8~3200`。

```bash
# 4) 转 om —— 注意：不使用 onnxsim
export SOC=Ascend910B4-1

cd /data/models/PP-OCRv5_server_det_infer/
atc --framework=5 --model=inference.onnx --output=det_final \
    --input_format=ND --input_shape="x:1,3,320~1408,320~1408" \
    --log=error --precision_mode_v2=origin --soc_version=$SOC
cp det_final_linux_aarch64.om inference.om

cd /data/models/PP-OCRv5_server_rec_infer/
atc --framework=5 --model=inference.onnx --output=rec_final \
    --input_format=ND --input_shape="x:1,3,48,8~3200" \
    --log=error --soc_version=$SOC
cp rec_final_linux_aarch64.om inference.om
```

> **atc 生成的文件带 ****`_linux_aarch64`**** 后缀,必须手动重命名为 ****`inference.om`****。**

### 11.6 产线配置

`/data/bench/OCR.yml`:

```yaml
pipeline_name: OCR
text_type: general
use_doc_preprocessor: False
use_textline_orientation: False
hpi_config:
  auto_config: False
  backend: om
  device_type: npu
SubPipelines:
  DocPreprocessor:
    pipeline_name: doc_preprocessor
    use_doc_orientation_classify: False
    use_doc_unwarping: False
    SubModules:
      DocOrientationClassify:
        module_name: doc_text_orientation
        model_name: PP-LCNet_x1_0_doc_ori
        model_dir: null
      DocUnwarping:
        module_name: image_unwarping
        model_name: UVDoc
        model_dir: null
SubModules:
  TextDetection:
    module_name: text_detection
    model_name: PP-OCRv5_server_det
    model_dir: /data/models/PP-OCRv5_server_det_infer
    limit_type: max
    limit_side_len: 960
  TextLineOrientation:
    module_name: textline_orientation
    model_name: PP-LCNet_x0_25_textline_ori
    model_dir: null
  TextRecognition:
    module_name: text_recognition
    model_name: PP-OCRv5_server_rec
    model_dir: /data/models/PP-OCRv5_server_rec_infer
    batch_size: 4
```

### 11.7 测试脚本

#### `bench.py` —— 单进程压测

```python
#!/usr/bin/env python3
import os, sys, time, json, glob, argparse
import numpy as np

def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--data",   default="/data/dataset/bids")
    ap.add_argument("--config", default="/data/bench/OCR.yml")
    ap.add_argument("--device", default="npu:0")
    ap.add_argument("--limit",  type=int, default=500)
    ap.add_argument("--warmup", type=int, default=30)
    ap.add_argument("--loop",   type=int, default=10)
    ap.add_argument("--tag",    default="run")
    ap.add_argument("--out",    default="/data/bench/results")
    args = ap.parse_args()

    from paddlex import create_pipeline

    files = sorted(glob.glob(os.path.join(args.data, "**", "*.png"), recursive=True))
    if not files:
        sys.exit(f"no png under {args.data}")
    files = (files * args.loop)[: args.limit + args.warmup]
    warm, work = files[:args.warmup], files[args.warmup:]

    pipeline = create_pipeline(pipeline=args.config, device=args.device, use_hpip=True)

    for f in warm:                       # 预热，不计时
        for _ in pipeline.predict(f):
            pass

    lat = []
    t0 = time.perf_counter()
    for f in work:
        s = time.perf_counter()
        for _ in pipeline.predict(f):    # 必须消费生成器才真正执行
            pass
        lat.append((time.perf_counter() - s) * 1000)
    total = time.perf_counter() - t0

    lat = np.array(lat)
    res = dict(tag=args.tag, pid=os.getpid(), device=args.device,
               n=len(work), wall_s=round(total, 3),
               qps=round(len(work) / total, 3),
               avg_ms=round(lat.mean(), 2),
               p50_ms=round(np.percentile(lat, 50), 2),
               p90_ms=round(np.percentile(lat, 90), 2),
               p99_ms=round(np.percentile(lat, 99), 2),
               max_ms=round(lat.max(), 2))
    os.makedirs(args.out, exist_ok=True)
    json.dump(res, open(f"{args.out}/{args.tag}_{os.getpid()}.json", "w"),
              ensure_ascii=False)
    print(json.dumps(res, ensure_ascii=False))

if __name__ == "__main__":
    main()
```

#### `regress.sh` —— 全量功能回归

```bash
#!/bin/bash
# 每次变更模型或配置后必须执行，通过率必须 100%
cd /data/bench
python3 - <<'PY'
import glob
from paddlex import create_pipeline
files = sorted(glob.glob("/data/dataset/bids/**/*.png", recursive=True))
p = create_pipeline(pipeline="/data/bench/OCR.yml", device="npu:0", use_hpip=True)
ok, bad, total_lines = 0, [], 0
for f in files:
    try:
        for res in p.predict(f):
            d = res.json.get("res", res.json) if isinstance(res.json, dict) else {}
            total_lines += len([t for t in d.get("rec_texts", []) if t.strip()])
        ok += 1
    except Exception as e:
        bad.append((f.split("/")[-1], str(e)[:50]))
print(f"\n通过 {ok}/{len(files)}   总文本行数 {total_lines}")
for f, e in bad[:10]:
    print("  FAIL", f, "|", e)
PY
```

> **通过率必须 100%,且总文本行数必须与基线一致。** 后者是完整性校验——只检查"不抛异常"会漏掉输出退化的情况。

#### `ramp.sh` —— 多实例爬坡

```bash
#!/bin/bash
# 用法: ./ramp.sh 1 4 8 12 16 20 24 32 38 44
set -u
OUT=/data/bench/results
USABLE=$(( $(nproc) / 2 ))          # 预留一半给系统
mkdir -p $OUT

# 环境断言 —— 避免静默失败
[ "${ASCEND_OM_OUTPUTSIZE:-}" = "64" ] || { echo "ERROR: OUTPUTSIZE 不为 64"; exit 1; }
[ -n "${SOC:-}" ] || { echo "ERROR: SOC 未设置"; exit 1; }

for N in "$@"; do
  echo ""; echo "════════ $N 实例 ════════"
  rm -f $OUT/ramp_${N}_i*.json
  PIDS=""
  CP=$((USABLE / N)); [ $CP -lt 1 ] && CP=1
  for i in $(seq 0 $((N-1))); do
    S=$((1 + i*CP)); E=$((S+CP-1))
    ASCEND_RT_VISIBLE_DEVICES=0 taskset -c $S-$E \
      python /data/bench/bench.py --limit 500 --loop 10 --warmup 30 \
      --out $OUT --tag ramp_${N}_i${i} > /dev/null 2>&1 &
    PIDS="$PIDS $!"
  done
  wait $PIDS                        # 只等压测进程
  python3 - "$N" "$OUT" <<'PY'
import glob, json, sys, numpy as np
N, OUT = sys.argv[1], sys.argv[2]
rs = [json.load(open(f)) for f in glob.glob(f"{OUT}/ramp_{N}_i*.json")]
rs = [r for r in rs if "qps" in r]
if not rs: print("  无数据"); sys.exit()
t = sum(r["qps"] for r in rs)
print(f"  存活 {len(rs)}/{N}   聚合 {t:.2f} QPS   单实例 {t/len(rs):.3f}   "
      f"avg {np.mean([r['avg_ms'] for r in rs]):.0f}ms   "
      f"P99max {max(r['p99_ms'] for r in rs):.0f}ms")
PY
  sleep 15
done
```

#### `monitor.sh` —— 资源采集

```bash
#!/bin/bash
# 用法: ./monitor.sh <tag> <npu_id>，Ctrl+C 或 kill 结束时输出汇总
TAG=${1:-run}; NPU=${2:-0}
OUT=/data/bench/results; mkdir -p $OUT
CSV=$OUT/${TAG}_monitor.csv
echo "ts,aicore,hbm_pct,cpu_cores" > $CSV
summarize() {
  python3 - "$CSV" "$OUT/${TAG}_res.json" <<'PY'
import sys, csv, json, statistics as st
rows = list(csv.DictReader(open(sys.argv[1])))
out = {}
for k in ["aicore", "hbm_pct", "cpu_cores"]:
    v = [float(r[k]) for r in rows if r[k] and float(r[k]) >= 0]
    if v: out[k] = dict(avg=round(st.mean(v),1), max=round(max(v),1),
                        p50=round(st.median(v),1))
out["samples"] = len(rows)
json.dump(out, open(sys.argv[2], "w"), indent=2)
print(json.dumps(out, indent=2))
PY
  exit 0
}
trap summarize INT TERM
while true; do
  L=$(npu-smi info -t usages -i $NPU -c 0 2>/dev/null)
  A=$(echo "$L" | grep -i "Aicore Usage Rate" | grep -oE "[0-9]+" | head -1)
  H=$(echo "$L" | grep -i "HBM Usage Rate"    | grep -oE "[0-9]+" | head -1)
  C=$(python3 -c "
import glob, time
def tot():
    t = 0
    for d in glob.glob('/proc/[0-9]*'):
        try:
            if 'bench.py' in open(d+'/cmdline','rb').read().decode('utf8','ignore'):
                f = open(d+'/stat').read().split(); t += int(f[13])+int(f[14])
        except: pass
    return t
a = tot(); time.sleep(1); b = tot()
v = (b-a)/100.0
print(round(v,3) if v >= 0 else '')
" 2>/dev/null)
  echo "$(date +%s),$A,$H,$C" >> $CSV
  sleep 1
done
```

> CPU 采样用 `/proc/<pid>/stat` 的差分而非 `ps %cpu`——后者是进程生命周期均值,会严重低估。差分为负时\(采样间隔内有进程退出\)丢弃该样本。

### 11.8 执行顺序

```bash
sudo docker exec -it paddlex bash
cd /data/bench

# ① 功能回归 —— 必须 100% 通过且总行数与基线一致
bash regress.sh

# ② 单实例基线 —— 至少 3 次，变异系数应 < 3%
for i in 1 2 3 4 5 6; do
  python bench.py --limit 500 --loop 10 --warmup 30 --tag base_r$i
done

# ③ 多实例爬坡（约 60~80 分钟，建议 tmux）
./monitor.sh ramp 0 &
./ramp.sh 1 4 8 12 16 20 24 32 38 44 2>&1 | tee ramp_log.txt
kill %1
```

### 11.9 预期结果对照

在 Ascend 910B4\-1 单卡上,应得到:

| 检查项         | 预期值          | 容差         |
| -------------- | --------------- | ------------ |
| 回归通过率     | 104/104         | 必须 100%    |
| 总文本行数     | 4661            | 必须完全一致 |
| 单实例 QPS     | 3.1 ~ 3.4       | ±10%         |
| 单实例 avg     | 294 ~ 320 ms    | ±10%         |
| 16 实例聚合    | 33.4 ~ 33.7     | ±3%          |
| 24 实例聚合    | 38.1 ~ 38.5     | ±3%          |
| **峰值聚合**   | **41.3 ~ 41.8** | **±3%**      |
| 实例上限       | 41 ~ 42         | ±2           |
| 16 实例 AICore | ~90%            | ±5%          |

**偏离超出容差时的排查顺序**:总文本行数 → 回归通过率 → 环境变量 → THP/governor → 机器是否有其他负载。

***

## 12. 局限性说明

### 12.1 样本量

数据集仅 104 张。单实例十余秒即可跑完,测试中通过 `--loop 10` 复用样本凑够 500 张的测试时长。

**已量化验证**:page cache 影响仅 +0.34%\(见 9.1\),该做法不引入显著偏差。

但需注意:计时集的 500 张中,图 10~29 这 20 张比其他图少跑一遍\(`(104×10)[:530]` 取整问题\)。该偏差在两台服务器上完全相同,不影响对照结论。

### 12.2 未覆盖的测试范围

| 项                 | 状态 | 说明                                                    |
| ------------------ | ---- | ------------------------------------------------------- |
| 整机多卡           | 未测 | 按 PL 指示不做。10.3 节的容量估算为线性外推             |
| 算力切分\(vNPU\)   | 未测 | 不在本版报告范围                                        |
| 服务化\(HTTP\)模式 | 未测 | 客户若为 HTTP 调用,需另行压测,离线脚本数据不可直接套用  |
| 长稳测试           | 未测 | 单轮最长约 3 分钟,未验证长时间运行的稳定性              |
| 参数敏感性         | 未测 | `limit_side_len`、`batch_size`、KPCV 开关的影响未做扫描 |

### 12.3 服务器 B 缺少资源采样

服务器 B 的爬坡仅记录了吞吐与时延,AICore/HBM/CPU 采样未成功保存。**第 6 节的瓶颈判定依据服务器 A 的资源数据**;由于两机吞吐曲线在高并发段高度重合\(±1.2%\),该判定可合理外推至服务器 B。

### 12.4 关于 onnxsim 的重要提示

昇腾社区《PaddleOCR优化参考实践》3.4 节建议对 rec 模型执行 `onnxsim` 简化。**实测该操作在动态 shape 场景下会导致功能性错误。**

现象:约 35%\(36/104\)的图片推理失败,报 ACL 500002。

根因:`onnxsim` 常量折叠时按静态输入假设推导形状,将某 reshape 输出固化为 `[40,40,120]`;运行时实际序列长度不同,与另一分支的 `[1600,1,120]` 无法广播,`Add` 算子 InferShape 失败:

```auth
[BroadcastShape] shape [1600, 1, 120] and [40, 40, 120] cannot broadcast!
OpName:[Add.38]
```

验证:同图 Paddle 后端正常输出 77 行;去掉 onnxsim 重转后 om 后端同样输出 77 行。

**结论:动态 shape 场景下不得使用 onnxsim。建议反馈至 PaddleX / CANN 侧。**

### 12.5 动态 shape 范围必须全量统计

初次基于前 30 张抽样得出 rec 输入宽度 max=1977,据此设上界 2048;全量 104 张实测 max 为 2185,导致超范围失败\(ACL 107000\)。

原因:文件按目录名排序,前 30 张全部落在第 1 个目录,未覆盖第 4 个目录的版式。

**结论:动态 shape 范围必须基于全量数据统计,数据集异质时抽样会系统性低估极值。**

***

## 13. 附录

### 13.1 指标定义

| 指标        | 计算方式                                           |
| ----------- | -------------------------------------------------- |
| QPS         | `计时图片数 / 整批墙钟秒数`                        |
| avg         | 单张耗时的算术平均                                 |
| P50/P90/P99 | 单张耗时排序后的分位数\(`np.percentile`,线性插值\) |
| 聚合 QPS    | `各实例 QPS 之和`\(非平均\)                        |
| 单实例 QPS  | `聚合 QPS / 存活实例数`                            |
| 扩展效率    | `聚合 QPS / (单实例基线 QPS × 实例数)`             |
| 边际收益    | `(QPS_n − QPS_{n-1}) / (n − n_{prev})`             |
| 变异系数 CV | `样本标准差 / 均值`                                |
| 字符一致率  | `difflib.SequenceMatcher.ratio() = 2M/T`           |

### 13.2 常见错误码

| 码       | 含义                  | 常见原因                                     |
| -------- | --------------------- | -------------------------------------------- |
| `-8005`  | DCMI 初始化失败       | 容器挂载不全 / 用户不在 HwHiAiUser 组        |
| `-8020`  | 设备被占用            | 其他容器或进程独占                           |
| `500000` | ACL 初始化失败        | 设备未正确绑定                               |
| `500002` | 模型执行失败,参数无效 | shape 超范围 / onnxsim 导致的 broadcast 失败 |
| `100000` | 内部错误              | 输出内存不足                                 |
| `107000` | 参数越界              | 输入尺寸超出 om 上界                         |

### 13.3 已知操作陷阱

| 陷阱                             | 说明                                                    |
| -------------------------------- | ------------------------------------------------------- |
| `atc` 输出带后缀                 | 生成 `xxx_linux_aarch64.om`,需手动改名为 `inference.om` |
| `ASCEND_OM_OUTPUTSIZE` 单位是 MB | **超出合法范围会静默回退到默认 64**,不报错              |
| 环境变量随容器销毁               | 写进 `docker run -e` 而非 `.bashrc`                     |
| `wait` 无参数                    | 会等待所有后台进程含监控脚本,须用 `wait $PIDS`          |
| `ps %cpu`                        | 是生命周期均值,不能用于瞬时 CPU 采样                    |
| 单张图通过 ≠ 可用                | 每次变更后必须跑全量回归并核对总文本行数                |

### 13.4 完整测试数据

**服务器 B 单实例 6 次**

| 轮次 | QPS   | avg    | P50    | P90    | P99    | max    |
| ---- | ----- | ------ | ------ | ------ | ------ | ------ |
| r1   | 3.191 | 313.34 | 320.54 | 439.58 | 624.59 | 698.36 |
| r2   | 3.091 | 323.46 | 332.94 | 449.37 | 691.53 | 739.24 |
| r3   | 3.133 | 319.13 | 326.89 | 444.27 | 632.08 | 734.93 |
| r4   | 3.084 | 324.22 | 331.71 | 453.16 | 631.44 | 862.60 |
| r5   | 3.108 | 321.71 | 332.08 | 448.09 | 627.84 | 783.22 |
| r6   | 3.128 | 319.68 | 326.64 | 447.11 | 627.66 | 740.54 |

**服务器 A 爬坡\(含资源\)**

| N    | 存活  | 聚合  | 单实例 | AICore | HBM   | avg  | P99max |
| ---- | ----- | ----- | ------ | ------ | ----- | ---- | ------ |
| 1    | 1/1   | 3.40  | 3.401  | 8.8%   | 6.8%  | 294  | 584    |
| 4    | 4/4   | 13.14 | 3.284  | 39.2%  | 14.0% | 304  | 598    |
| 8    | 8/8   | 22.21 | 2.776  | 73.6%  | 23.9% | 360  | 705    |
| 16   | 16/16 | 33.37 | 2.086  | 89.7%  | 43.0% | 479  | 953    |
| 24   | 24/24 | 38.10 | 1.588  | 90.2%  | 60.9% | 630  | 1568   |
| 32   | 32/32 | 39.22 | 1.226  | 92.8%  | 82.2% | 816  | 1806   |
| 38   | 38/38 | 41.83 | 1.101  | 92.8%  | 88.5% | 910  | 2180   |
| 44   | 42/44 | 41.70 | 0.993  | 86.0%  | 88.4% | 1010 | 2547   |

**服务器 B 爬坡**

| N    | 存活  | 聚合  | 单实例 | avg  | P99max |
| ---- | ----- | ----- | ------ | ---- | ------ |
| 1    | 1/1   | 3.14  | 3.143  | 318  | 706    |
| 4    | 4/4   | 11.97 | 2.993  | 334  | 681    |
| 8    | 8/8   | 21.42 | 2.677  | 374  | 768    |
| 12   | 12/12 | 28.41 | 2.368  | 423  | 1005   |
| 16   | 16/16 | 33.67 | 2.104  | 476  | 1059   |
| 20   | 20/20 | 36.74 | 1.837  | 545  | 1424   |
| 24   | 24/24 | 38.54 | 1.606  | 623  | 1497   |
| 32   | 32/32 | 39.42 | 1.232  | 812  | 1915   |
| 38   | 38/38 | 41.33 | 1.088  | 920  | 2247   |
| 44   | 41/44 | 41.72 | 1.017  | 985  | 2949   |

### 13.5 图表清单

| 图   | 文件                             | 内容                   |
| ---- | -------------------------------- | ---------------------- |
| 图 1 | `figs_v2/v2_fig1_scaling.png`    | 双机吞吐爬坡对照       |
| 图 2 | `figs_v2/v2_fig2_efficiency.png` | 扩展效率               |
| 图 3 | `figs_v2/v2_fig3_marginal.png`   | 边际收益递减           |
| 图 4 | `figs_v2/v2_fig4_tradeoff.png`   | 吞吐\-时延权衡         |
| 图 5 | `figs_v2/v2_fig5_resource.png`   | 资源利用率与瓶颈       |
| 图 6 | `figs_v2/v2_fig6_single.png`     | 单实例稳定性与时延对照 |
| 图 7 | `figs_v2/v2_fig7_stage.png`      | 单页耗时构成           |

***

*本报告数据均为实测。原始 JSON 与 CSV 保存于各服务器 \(西安绿区 110.21.1.\)**`/data/bench/results\*/`** 目录。*
